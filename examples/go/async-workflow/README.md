# async-workflow

Chains two asynchronous command steps via ``StartCommand`` and ``WaitForHandle``
on a local sandbox.

## Prerequisites

- Go toolchain and Docker; run from ``examples/go``.

## How to run

```bash
cd examples/go
go run ./async-workflow
```

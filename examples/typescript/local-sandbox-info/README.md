# local-sandbox-info

Reads sandbox metadata from the create response and again via ``Info`` /
``info``. Helps you confirm identifiers and status while debugging lifecycle
issues.

## Prerequisites

- Node.js 18 or newer (or Bun) and Docker.

## How to run

```bash
npx tsx examples/typescript/local-sandbox-info/index.ts
```

# async-workflow

Uses ``Promise.all`` and sequential ``await`` to run multiple commands in one
script; demonstrates async orchestration in JavaScript.

## Prerequisites

- Node.js 18 or newer (or Bun) and Docker.

## How to run

```bash
npx tsx examples/typescript/async-workflow/index.ts
```
